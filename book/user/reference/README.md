# Reference

This chapter is for looking things up, not for reading from the top.

It lists every option and subcommand of `plc`, every key of the project file, the function families of the standard library with the file that declares each of them, and every diagnostic code with an example of the mistake and of the correct form, which is the same text that `plc explain` prints.

The guide chapters explain the concepts behind all of it.
