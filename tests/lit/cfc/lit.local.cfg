# CFC integration tests: a `.cfc` unit under test plus an ST harness, compiled
# together and run, with the result checked via `%RUN | %CHECK`. Each test is a
# `.test` driver (RUN/CHECK directives).
config.suffixes = ['.test']
