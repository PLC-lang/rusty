stdlibLocation = lit_config.params["LIB"]
compilerLocation = lit_config.params["COMPILER"]
# Add a substitution for a lib compile command
libCompile = f'{compilerLocation} build'
libCompile = f'{libCompile} --linker=cc'
print(f'Lib Compile command: {libCompile}')

config.substitutions.append(('%LIBCOMPILE', f'{libCompile}'))
config.substitutions.append(('%APPRUN', f'LD_LIBRARY_PATH="{stdlibLocation}/lib:./build" /tmp/%basename_t.out'))
