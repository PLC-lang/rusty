[package]
name = "plc_literals"
version = "1.1.0-dev"
edition = "2021"
license = "LGPL-3.0"
description = "Allocation-free parsers for Structured Text literals, shared by the compiler and the standard library"

[dependencies]
