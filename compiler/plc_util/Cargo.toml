[package]
name = "plc_util"
version = "0.4.0"
edition = "2021"
license = "LGPL-3.0"
description = "Shared utilities for the PLC Structured Text compiler"

[dependencies]
insta = { version = "1", features = ["filters"] }
