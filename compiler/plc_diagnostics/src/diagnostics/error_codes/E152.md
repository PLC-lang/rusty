# Unconnected CFC EN pin

A block in a CFC network declares execution control but its EN pin carries no
connection. Without a wired enable the guard can never be decided, so the call
is rejected; unlike an unwired data input there is no sensible default.
