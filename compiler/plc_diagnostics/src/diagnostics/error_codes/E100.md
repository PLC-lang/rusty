# Reassignment of alias variables

Reassigning alias variables is disallowed, because ...
For example the following code will return an error
```ST
FUNCTION main
    VAR
        foo AT bar : DINT;
        bar : DINT;
        baz : DINT;
    END_VAR

    foo REF= baz; // Reassignment error
END_FUNCTION
```