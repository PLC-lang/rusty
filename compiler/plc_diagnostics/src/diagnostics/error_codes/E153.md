# CFC ENO cycle

An ENO pin mirrors its block's EN value, so a wire reading ENO resolves through
the block to the EN source. When the EN pins of two or more blocks are fed only
by each other's ENO pins, the wire never reaches a real source and the blocks
are rejected.
