name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name : Cargo Docker build  with LLVM-10
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Cargo Build and Test
      uses: ./docker-build/
      with:
        build-step: 'test'
    # - name: rust-tarpaulin
    #   uses: actions-rs/tarpaulin@v0.1
    # - name: Codecov
    #   uses: codecov/codecov-action@v1.1.1
    #   with:
    #     token: ${{ secrets.CODECOV_TOKEN }}
